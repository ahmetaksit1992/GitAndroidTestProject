package com.example.gittest

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
    }
}

//todo 6:15 of video explains how to do stuff on repository --> dont have
//todo after having access to repo (github) VCS -> Git -> Push
//todo master -> define remote (URL to your repo)

//todo File -> new -> project from version control -> URL (put UltimateAndroidReference repo link)

//todo MERGE:
//todo 